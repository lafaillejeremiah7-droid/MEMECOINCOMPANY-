name: Secret pattern guard

on:
  pull_request:
  push:

permissions:
  contents: read

jobs:
  scan-current-tree:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - name: Scan committed provider credential patterns
        run: python3 scripts/check_secrets.py
